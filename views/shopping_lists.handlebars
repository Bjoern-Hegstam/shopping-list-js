<div class="container">
    <div class="row">
        {{#each shopping_list}}
        <div class="col-sm-4">
            <a class="btn btn-link btn-block" href="/shopping-list/{{id}}">
                <h2>
                    {{name}}
                </h2>
            </a>
        </div>
        {{/each}}
    </div>
</div>
