<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <a href="#" class="navbar-brand">shopping-list-js</a>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row">
        {{#each data}}
        <div class="col-sm-4">
            <a class="btn btn-link btn-block" href="/shopping_list/{{id}}">
                <h2>
                    {{attributes.name}}
                </h2>
            </a>
        </div>
        {{/each}}
    </div>
</div>
